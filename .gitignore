# Dependencies & build outputs
node_modules
dist

# Local data (SQLite + WAL)
data

# Environment files — keep `.env.example` tracked, ignore the rest
.env
.env.local
.env.*.local
.env.development
.env.production

# Logs
*.log
logs

# Test artifacts
coverage
.nyc_output

# Editor / OS
.DS_Store
.idea
.vscode
*.swp
*.swo

# Project-local Claude config (per-machine, not for the repo)
.claude/settings.local.json
.claude/projects

# Local-only docs / experiments — see internal/byok-browser-plan.md etc.
archive
internal

